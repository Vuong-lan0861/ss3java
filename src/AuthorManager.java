import java.util.ArrayList;
import java.util.Scanner;

public class AuthorManager {
    static void manageAuthors(Scanner scanner) {
        // Các phương thức quản lý tác giả nằm ở đây
    }

    // Các phương thức quản lý tác giả (addAuthor, displayAuthors, updateAuthor, deleteAuthor) nằm ở đây
}
